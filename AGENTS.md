# AI Collaboration Rules

This repo uses AI coding companions. To reduce drift:

- Prefer small, incremental changes.
- Update docs in `docs/` when behavior changes.
- Keep file naming and structure consistent with `docs/index.md`.
- Avoid large refactors without explicit approval.
