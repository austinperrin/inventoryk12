# Routing Configuration Templates

This directory contains templates for tenant/environment routing configuration.

- These templates are deployment and edge-routing inputs.
- They are intentionally separate from Django runtime `.env` settings.
- Keep values non-secret in committed examples.

Current template:

- `tenant-environment-routing.example.yaml`
