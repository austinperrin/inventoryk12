# Configs

This directory contains runtime configuration templates and supporting config files.

## Current Structure

- `configs/env/`: local environment file templates used by bootstrap scripts.

## Boundaries

- Security and governance policy documentation lives in `docs/security/policies/`.
- Do not store secrets in this directory.
